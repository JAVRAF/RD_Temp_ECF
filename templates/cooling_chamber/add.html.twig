{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        {% if isAdded == false %}
            <h1>Add a new chamber</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="button" type="submit" value="Submit"/>
            {{ form_end(form) }}
        {% else %}
            <h1>The cooling chamber {{ chamber.name }} has been created ! ✅</h1>
            <button onclick="window.location.href = '{{ path('app_coolingchamber_add') }}'">Add a new cooling chamber</button>
            <button onclick="window.location.href = '{{ path('app_coolingchamber_list') }}'">Back to cooling chamber list</button>
        {% endif %}
    </div>
{% endblock %}

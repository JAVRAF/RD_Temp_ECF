{% extends 'base.html.twig' %}

{% block body %}
    <div class="example-wrapper">
        {% if isedited == false %}
            <h1>Edit {{ store.username }}</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="button" type="submit" value="edit store"/>
            {{ form_end(form) }}
            <button onclick="window.location.href = '{{ path('app_store_list') }}'">Back to store list</button>
        {% else %}
            <h1>The
                store {{ store.username }} has been edited! ✅</h1>
            <button onclick="window.location.href = '{{ path('app_store_list') }}'">Back to store list</button>
        {% endif %}
    </div>
{% endblock %}

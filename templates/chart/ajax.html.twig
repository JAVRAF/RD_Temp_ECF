{% extends 'base.html.twig' %}
{% block header %}

{% endblock %}

{% block body %}
    <style>
        canvas {
            border: solid black 1px
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>
    <div>
        <h2>temp values:</h2>
        {% for v in tempdata %}
            <p>{{ v.2 }} : {{ v.3 }}</p>
        {% endfor %}
    </div>
    <div>
        <h2>hygro values:</h2>
        {% for v in hygrodata %}
            <p>{{ v.2 }} : {{ v.3 }}</p>
        {% endfor %}
    </div>
{% endblock %}


